# 올페이즈 결제 대시보드 개발 가이드

### 주요 페이지

- **📊 대시보드**: 실시간 거래 통계, KPI, 거래 추이 차트
- **📝 거래 내역 조회**: 기간별 거래내역 조회, 필터링(상태, 결제수단, 가맹점)

---

## 🚀 환경 및 로컬에서 시작하기

### 환경 요구사항

```bash
Node.js: 20.x LTS (예: 20.11.0, 20.15.0)
npm: Node.js 기본 포함 버전

# 확인
node --version    # v20.x 이상
npm --version     # 10.x 이상
```

### 설치 및 실행

```bash
# 저장소 클론
git clone <repository-url>
cd payment-dashboard

# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 브라우저에서 http://localhost:5173 접속
```

---

## 🛠 기술 스택

| 분류           | 기술                                   |
| -------------- | -------------------------------------- |
| **프레임워크** | React 18.3+, TypeScript 5.3+           |
| **빌드 도구**  | Vite 5.0+                              |
| **상태 관리**  | Jotai (원자 단위 상태 관리)            |
| **테이블**     | TanStack React Table v8                |
| **차트**       | ReCharts v3                            |
| **스타일링**   | Tailwind CSS 3.3+ (자체 디자인 시스템) |
| **HTTP 통신**  | Axios 1.6+                             |
| **날짜 처리**  | date-fns 2.30+                         |

---

## 💡 주요 기능

### 1. 거래 내역 조회

- 📅 **기간 필터**: 헤더의 CalendarModal을 통한 직관적 기간 선택
- 🔍 **다중 필터**: 결제상태, 결제수단, 가맹점
- 📊 **페이지네이션**: 10/20/30/40/50건 선택 가능
- ✅ **정확한 경계값 처리**: 10.31 ~ 11.04 선택 시 양쪽 날짜 모두 포함

### 2. 대시보드

- 📈 실시간 거래 통계 및 차트
- 🏪 가맹점별 거래 현황
- 📝 최근 거래 목록

### 3. 상태 관리

- **Jotai 기반 전역 상태**: 헤더의 기간 설정이 모든 페이지에 자동 동기화
- **정규화된 데이터**: mchtCode로 조회 성능으로 최적화

---

## 🎨 스타일 출처 및 디자인 의도

### 스타일링

**Tailwind CSS 3.3+** 를 기반 **아토믹 디자인 패턴으로** 재사용성 있게 구축했습니다.

- 색상 토큰 기반 시스템
- 아토믹 디자인 기반 컴포넌트 아키텍처

### 디자인 의도 및 주요 UI/UX 포인트

1. **PG 도메인 정보 구조화**: 숫자 정보가 많은 결제 데이터를 쉽게 파악할 수 있도록 KPI 추세, 거래 내역 리스트를 구분하여 대시보드에 배치했습니다.

2. **사용자 흐름 기반 레이아웃**: ‘기간 선택 → 필터링 → 결과 확인 → 상세’의 실제 업무 흐름을 기준으로 화면을 구성해 최소 조작으로 원하는 정보를 탐색할 수 있도록 설계했습니다

3. **시각적으로 정돈된 대시보드**: 지정 컬러 팔레트, 간격을 정의해두고, 통일된 컴포넌트 스타일을 적용해 부담 없이 읽을 수 있도록 구현했습니다.
4. **접근성 고려한 설계**: 키보드 네비게이션, 시맨틱 태그 등을 적용해 접근성 확보에 신경썼습니다.

---

## 📦 주요 라이브러리

| 라이브러리           | 용도                   |
| -------------------- | ---------------------- |
| Jotai                | 원자 단위 상태 관리    |
| TanStack React Table | 효율적인 테이블 렌더링 |
| Tailwind CSS         | 유틸리티 기반 스타일링 |
| Axios                | RESTful API 통신       |
| date-fns             | 날짜 및 시간 처리      |

---

## 🎯 핵심 구현

### 정확한 기간 필터링

```typescript
const getDateRange = () => {
  const start = new Date(headerStartDate);
  start.setHours(0, 0, 0, 0);

  const end = new Date(headerEndDate);
  end.setHours(23, 59, 59, 999);

  return { start, end };
};
```

### 전역 상태 동기화

```typescript
const { startDate: headerStartDate, endDate: headerEndDate } = useAtomValue(dateRangeAtom);
```

### 타입 안전 필터링

```typescript
interface TransactionFilters {
  merchantId: string | null;
  status: 'ALL' | 'SUCCESS' | 'FAILED' | 'PENDING';
  payType: 'ALL' | string;
}
```

---

**마지막 업데이트**: 2025년 12월 5일
